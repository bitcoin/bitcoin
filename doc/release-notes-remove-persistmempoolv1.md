Updated settings
----------------

- The `-persistmempoolv1` startup option has been removed. It was introduced
  as a temporary option in v27.0 to allow downgrading to v26.x and earlier,
  which cannot read the current `mempool.dat` format.
